#ifndef IMAGES_NOTES_HPP
#define IMAGES_NOTES_HPP

/*
 * Данный набор объектов представляет опорные точки для рисования нот.
 * Все объекты нот содержат помимо методов х и у координат
 * методы uint16_t x_stem(bool) и uint16_t y_stem(bool) возвращающие координаты для рисования штиля (0 - штиль вниз, 1 -
 * штиль вверх).
 */

#include "note.hpp"
#include "note_1.hpp"

#endif // IMAGES_NOTES_HPP
